import React, { Component, PropTypes } from 'react';

export default class ReactHover extends Component {
  static propTypes = {
    
  }

  constructor(props) {
    super(props);

    this.state = {
    }
  }

  render() {
    return (
      <div>
        hello React!
      </div>
    );
  }
}
